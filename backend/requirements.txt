# backend/requirements.txt
# Pinned after a working install — update with `pip freeze` after each dep change.

# --- Web framework ---
fastapi==0.115.5
uvicorn[standard]==0.32.1
gunicorn==23.0.0
websockets==13.1

# --- Config / env ---
python-dotenv==1.0.1
pydantic-settings==2.6.1

# --- LangChain stack ---
# FIX: removed langchain-classic (wrong package — those functions live in langchain)
langchain==0.3.7
langchain-core==0.3.19
langchain-openai==0.2.9
langchain-community==0.3.7
langchain-chroma==0.1.4          # FIX: was langchain-Chroma (wrong capitalisation)
langchain-text-splitters==0.3.2

# --- LangGraph ---
langgraph==0.2.55

# --- Vector store ---
chromadb==0.5.20

# --- Document loading ---
pypdf==5.1.0

# --- AI / reliability ---
openai==1.54.4
tenacity==9.0.0

# --- Auth ---
pyjwt==2.10.0